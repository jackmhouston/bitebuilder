# src quarantine note

The canonical BiteBuilder runtime for the current stabilization window lives in the top-level app:

- `bitebuilder.py`
- `webapp.py`
- `parser/`, `generator/`, and `llm/`

The `src/bitebuilder/` package is kept only for inventory/reference until an explicit migration or removal decision is made. Do not treat it as the active implementation path.
